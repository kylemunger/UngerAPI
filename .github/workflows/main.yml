name: CI/CD

on:
  push:
    branches:
    - main

jobs:
  setup-build-publish-deply:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to Google Container Registry
      uses: docker/login-action@v1
      with:
        registry: gcr.io
        username: _json_key
        password: ${{ secrets.GCLOUD_SERVICE_ACCOUNT_KEY }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: ./UngerApi.Minimal/
        push: true
        tags: grc.io/${{ vars.GKE_PROJECT}}/ungerapi:latest

    - name: Setup gcloud CLI
      uses: google-github-actions/setup-gcloud@v1
      with:
        service_account_key: ${{ secrets.GCLOUD_SERVICE_ACCOUNT_KEY }}
        project_id: ${{ vars.GKE_PROJECT }}
    
    - name: Configure Docker to use the gcloud command-line tool as a credential helper
      run: |-
        gcloud --quiet auth configure-docker

    - name: Deploy to GKE
      run: |-
        gcloud container clusters get-credentials ${{ vars.GKE_CLUSTER }} --zone ${{ env.GKE_ZONE }}
        kubectl apply -f /UngerApi/deploy.yaml

    